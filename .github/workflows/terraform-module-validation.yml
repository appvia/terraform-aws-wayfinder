name: Terraform Module Validation
on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  terraform:
      uses: appvia/appvia-cicd-workflows/.github/workflows/terraform-module-validation.yml@infracost
      name: Terraform Module Validation
      secrets:
        infracost-api-key: ${{ secrets.ORG_INFRACOST_API_KEY }}
